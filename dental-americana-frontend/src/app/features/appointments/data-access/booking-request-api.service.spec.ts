import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{BookingRequestApiService}from'./booking-request-api.service';describe('BookingRequestApiService',()=>{let service:BookingRequestApiService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(BookingRequestApiService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('does not create an appointment directly from the public form',()=>{const payload={fullName:'Ana Torres',documentNumber:null,mobile:'999999999',email:null,service:'Evaluación general',preferredDate:null,preferredShift:'INDIFERENTE',message:null,privacyConsent:true};service.publicCreate(payload).subscribe();const r=http.expectOne('/api/v1/public/appointment-requests');expect(r.request.body).toEqual(payload);r.flush({});});it('links a reviewed request to an existing appointment',()=>{service.manage(8,'AGENDADO',2,'Validado',44).subscribe();const r=http.expectOne('/api/v1/appointment-requests/8');expect(r.request.body).toEqual({status:'AGENDADO',version:2,observation:'Validado',appointmentId:44});r.flush({});});});
