import{DatePipe}from'@angular/common';import{HttpErrorResponse}from'@angular/common/http';import{ChangeDetectionStrategy,Component,OnInit,inject,signal}from'@angular/core';import{RouterLink}from'@angular/router';import{LucideAlertTriangle,LucideArrowLeft,LucideCalendarCheck,LucideCheck,LucideClock3,LucideLoaderCircle,LucideMail,LucideMessageSquare,LucidePhone,LucideRefreshCw,LucideTrash2,LucideUserCheck}from'@lucide/angular';import{BookingRequestApiService}from'../../data-access/booking-request-api.service';import{BookingRequest,BookingRequestStatus}from'../../models/booking-request.models';
@Component({selector:'app-booking-requests',imports:[RouterLink,DatePipe,LucideAlertTriangle,LucideArrowLeft,LucideCalendarCheck,LucideCheck,LucideClock3,LucideLoaderCircle,LucideMail,LucideMessageSquare,LucidePhone,LucideRefreshCw,LucideTrash2,LucideUserCheck],templateUrl:'./booking-requests.html',styleUrl:'./booking-requests.css',changeDetection:ChangeDetectionStrategy.OnPush})export class BookingRequests implements OnInit{private readonly api=inject(BookingRequestApiService);readonly items=signal<BookingRequest[]>([]);readonly loading=signal(true);readonly saving=signal(false);readonly filter=signal<BookingRequestStatus|''>('');readonly error=signal('');readonly success=signal('');ngOnInit(){this.load();}load(){this.loading.set(true);this.api.list(this.filter()||undefined).subscribe({next:r=>{this.items.set(r);this.loading.set(false);},error:()=>{this.loading.set(false);this.error.set('No se pudieron cargar las solicitudes web.');}});}setFilter(v:string){this.filter.set(v as BookingRequestStatus|'');this.load();}manage(item:BookingRequest,status:BookingRequestStatus){let observation:string|undefined;let appointmentId:number|undefined;if(status==='CONTACTADO')observation=prompt('Observación del contacto:')?.trim()||undefined;if(status==='DESCARTADO'){observation=prompt('Motivo para descartar:')?.trim();if(!observation)return;}if(status==='AGENDADO'){const value=prompt('Ingresa el ID de la cita creada en la agenda:');if(!value||!Number(value))return;appointmentId=Number(value);observation='Solicitud vinculada con la cita creada';}this.saving.set(true);this.api.manage(item.id,status,item.version,observation,appointmentId).subscribe({next:()=>{this.saving.set(false);this.success.set('Solicitud actualizada.');this.load();},error:(e:HttpErrorResponse)=>{this.saving.set(false);this.error.set(e.error?.message||'No se pudo actualizar la solicitud.');}});}label(v:string){return v.replaceAll('_',' ').toLowerCase();}}
