import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{AdminApiService}from'./admin-api.service';describe('AdminApiService',()=>{let service:AdminApiService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(AdminApiService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('updates a setting with optimistic version',()=>{service.updateSetting('clinica.telefono','940 577 075',4).subscribe();const r=http.expectOne('/api/v1/admin/settings/clinica.telefono');expect(r.request.body).toEqual({value:'940 577 075',version:4});r.flush({});});});
