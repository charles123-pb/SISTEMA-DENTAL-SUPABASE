import{DatePipe}from'@angular/common';import{HttpErrorResponse}from'@angular/common/http';import{ChangeDetectionStrategy,Component,OnInit,computed,inject,signal}from'@angular/core';import{LucideAlertTriangle,LucideBot,LucideCheck,LucideClipboardCheck,LucideClock3,LucideFileHeart,LucideHistory,LucideLoaderCircle,LucideRefreshCw,LucideShieldCheck,LucideSparkles,LucideStethoscope,LucideX}from'@lucide/angular';import{Observable}from'rxjs';import{AuthService}from'../../../../core/auth/auth.service';import{ClinicalApiService}from'../../../clinical/data-access/clinical-api.service';import{ClinicalEncounter}from'../../../clinical/models/clinical.models';import{CopilotApiService}from'../../data-access/copilot-api.service';import{AiDraft,AiDraftType}from'../../models/copilot.models';
@Component({selector:'app-copilot-workspace',imports:[DatePipe,LucideAlertTriangle,LucideBot,LucideCheck,LucideClipboardCheck,LucideClock3,LucideFileHeart,LucideHistory,LucideLoaderCircle,LucideRefreshCw,LucideShieldCheck,LucideSparkles,LucideStethoscope,LucideX],templateUrl:'./copilot-workspace.html',styleUrl:'./copilot-workspace.css',changeDetection:ChangeDetectionStrategy.OnPush})export class CopilotWorkspace implements OnInit{private readonly api=inject(CopilotApiService);private readonly clinical=inject(ClinicalApiService);private readonly auth=inject(AuthService);readonly encounters=signal<ClinicalEncounter[]>([]);readonly selected=signal<ClinicalEncounter|null>(null);readonly drafts=signal<AiDraft[]>([]);readonly loading=signal(true);readonly working=signal(false);readonly error=signal('');readonly success=signal('');readonly canGenerate=computed(()=>this.auth.hasPermission('IA_ESCRIBIR'));readonly canApprove=computed(()=>this.auth.hasPermission('IA_APROBAR'));readonly draftTypes:AiDraftType[]=['RESUMEN_HISTORIA','BORRADOR_EVOLUCION','INDICACIONES_POSTCONSULTA','VERIFICACION_CIERRE'];ngOnInit(){this.loadEncounters();}loadEncounters(){this.loading.set(true);this.error.set('');const to=new Date();to.setDate(to.getDate()+1);const from=new Date();from.setDate(from.getDate()-89);this.clinical.search(from.toISOString(),to.toISOString()).subscribe({next:r=>{this.encounters.set(r);this.loading.set(false);if(r.length)this.select(r[0]);},error:()=>{this.loading.set(false);this.error.set('No se pudieron cargar las atenciones recientes.');}});}select(e:ClinicalEncounter){this.selected.set(e);this.loading.set(true);this.api.list(e.id).subscribe({next:r=>{this.drafts.set(r);this.loading.set(false);},error:()=>{this.loading.set(false);this.error.set('No se pudieron cargar los borradores.');}});}generate(type:AiDraftType){const e=this.selected();if(!e)return;this.run(this.api.generate(e.id,type),'Borrador generado desde datos registrados.');}approve(d:AiDraft){if(!confirm('¿Confirmas que revisaste el contenido y deseas aprobarlo profesionalmente?'))return;this.run(this.api.approve(d.id,d.version),'Borrador aprobado y registrado en auditoría.');}reject(d:AiDraft){const reason=prompt('Indica brevemente qué debe corregirse:');if(!reason?.trim())return;this.run(this.api.reject(d.id,d.version,reason.trim()),'Borrador rechazado.');}label(v:string){return v.replaceAll('_',' ').toLowerCase();}icon(type:AiDraftType){return type;}private run(request:Observable<AiDraft>,message:string){this.working.set(true);this.error.set('');this.success.set('');request.subscribe({next:()=>{this.working.set(false);this.success.set(message);const e=this.selected();if(e)this.select(e);},error:(e:HttpErrorResponse)=>{this.working.set(false);this.error.set(e.error?.message||'No se pudo completar la acción.');}});}}
