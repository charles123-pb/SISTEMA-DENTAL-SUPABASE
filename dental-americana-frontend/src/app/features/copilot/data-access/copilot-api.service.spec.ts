import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{CopilotApiService}from'./copilot-api.service';describe('CopilotApiService',()=>{let service:CopilotApiService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(CopilotApiService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('generates a traceable draft for an encounter',()=>{service.generate(12,'VERIFICACION_CIERRE').subscribe();const r=http.expectOne('/api/v1/copilot/drafts');expect(r.request.body).toEqual({encounterId:12,type:'VERIFICACION_CIERRE'});r.flush({});});it('sends optimistic version on professional approval',()=>{service.approve(5,2).subscribe();const r=http.expectOne('/api/v1/copilot/drafts/5/approve');expect(r.request.body).toEqual({version:2});r.flush({});});});
