import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{MessagingApiService}from'./messaging-api.service';describe('MessagingApiService',()=>{let service:MessagingApiService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(MessagingApiService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('marks a follow-up as professionally reviewed',()=>{service.review(9,3).subscribe();const r=http.expectOne('/api/v1/messaging/follow-ups/9/review');expect(r.request.method).toBe('PATCH');expect(r.request.body).toEqual({version:3,professionalReview:true});r.flush({});});});
