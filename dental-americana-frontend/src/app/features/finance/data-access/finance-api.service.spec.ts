import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{FinanceApiService}from'./finance-api.service';describe('FinanceApiService',()=>{let service:FinanceApiService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(FinanceApiService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('requires explicit confirmation when registering a payment',()=>{service.pay(7,2,150,'YAPE-1').subscribe();const r=http.expectOne('/api/v1/finance/payments');expect(r.request.body).toEqual({accountId:7,paymentMethodId:2,amount:150,reference:'YAPE-1',confirmation:true});r.flush({});});it('requires confirmation and version when closing cash',()=>{service.closeCash(3,480,'Conforme',5).subscribe();const r=http.expectOne('/api/v1/finance/cash/3/close');expect(r.request.body).toEqual({countedAmount:480,observation:'Conforme',version:5,confirmation:true});r.flush({});});});
