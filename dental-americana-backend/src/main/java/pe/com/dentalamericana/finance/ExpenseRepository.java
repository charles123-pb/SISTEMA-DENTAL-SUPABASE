package pe.com.dentalamericana.finance;import org.springframework.data.jpa.repository.JpaRepository;import java.time.Instant;import java.util.List;public interface ExpenseRepository extends JpaRepository<Expense,Long>{List<Expense>findAllByRegisteredAtBetweenOrderByRegisteredAtDesc(Instant from,Instant to);List<Expense>findAllByCashSessionIdAndEstado(Long id,FinancialRecordStatus status);}
