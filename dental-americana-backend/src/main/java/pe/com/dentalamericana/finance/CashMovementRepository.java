package pe.com.dentalamericana.finance;import org.springframework.data.jpa.repository.JpaRepository;import java.time.Instant;import java.util.List;public interface CashMovementRepository extends JpaRepository<CashMovement,Long>{List<CashMovement>findAllByCashSessionIdOrderByCreatedAtDesc(Long id);List<CashMovement>findAllByCreatedAtBetweenOrderByCreatedAtDesc(Instant from,Instant to);}
