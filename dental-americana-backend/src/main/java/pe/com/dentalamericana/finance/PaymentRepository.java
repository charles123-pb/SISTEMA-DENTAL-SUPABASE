package pe.com.dentalamericana.finance;import org.springframework.data.jpa.repository.JpaRepository;import java.time.Instant;import java.util.List;public interface PaymentRepository extends JpaRepository<Payment,Long>{List<Payment>findAllByRegisteredAtBetweenOrderByRegisteredAtDesc(Instant from,Instant to);List<Payment>findAllByCashSessionIdAndEstado(Long id,FinancialRecordStatus status);}
